import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      title: 'Hello wolrd',
      home: Scaffold(
        appBar: AppBar(title: Text('Hello Flutter')),
        body: Text("Hello Flutter s"),
      ),
    ));
